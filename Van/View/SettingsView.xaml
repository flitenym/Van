<UserControl x:Class="Van.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Van.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries> 
                <ResourceDictionary Source="/Themes/Blue/Blue.xaml" x:Name="blue"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>


    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" CanVerticallyScroll="True" Orientation="Vertical" Margin="5">
        <TextBlock Style="{StaticResource MaterialDesignTitleTextBlock}" Text="Темы" HorizontalAlignment="Center"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="45" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Grid Grid.Column="0" Grid.Row="0" VerticalAlignment="Top">
                <Button x:Uid="Blue" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{StaticResource MaterialDesignToolForegroundButton}" Command="{Binding ThemeAcceptor}" >
                    <materialDesign:PackIcon Kind="Drop" Width="35" Height="Auto" Foreground="{Binding Source={StaticResource BlueButton}}"/>
                </Button>
            </Grid>
            <Expander Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch" Header="Синий" materialDesign:ExpanderAssist.DownHeaderPadding="35,2,24,2">
                <StackPanel Orientation="Vertical" TextBlock.Foreground="{DynamicResource MaterialDesignBody}" Margin="24,8,24,16">
                    <TextBlock Text="Здесь будет показ темы"/>
                </StackPanel>
            </Expander>
        </Grid>
    </StackPanel>
</UserControl>
